/**
 * Centralized fallback values for highlight marks. Imported by the DOM
 * layer and referenced in `HighlightOptions` JSDoc — change once, here.
 */

export const DEFAULT_TEXT_COLOR = "#1C252E";
export const DEFAULT_BG_COLOR = "#FFD666";
